fast, fast
unordered read
config files
- map filenames to node
- extract and count/sum/avg (per minute?) or keep as exact.
debug per config file

x - output (timestamp, host, resource, action, value)

validate configs

x - init multiple value handling

x - actual output

input options handling

min/max dateTime

granularity (s, min, h)?

message counts (and per node)?  is this useful?
